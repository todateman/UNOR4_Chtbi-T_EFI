# インジェクタ 無効噴射時間・流量 特性測定手順書

対象: Chibi-T ECU（UNOR4）用インジェクタ
関連リポジトリ: `UNOR4_Chtbi-T_EFI`（`develop/2026zenkoku` / `INJ_test`ブランチ）

## 1. 目的

インジェクタの噴射特性を次の一次式でモデル化し、実測により定数を決定する。

```txt
噴射量 V = 流量Q × (指令パルス幅 PW − 無効噴射時間 t_dead)
```

- **流量 Q**：単位時間あたりの燃料噴射量（ml/ms や g/min）
- **無効噴射時間 t_dead**：電圧印加から実際に弁が開くまでの実効遅れ（ms）

現状 `main.cpp` の `gasml += (((...)*0.0000007)+0.0015)/1.5073;` は、過去の実測シート（`燃料噴射時間・量`）の質量データを線形回帰した式（`0.0007*PW+0.0015`）を比重換算し、さらに出所不明の `1.5073` で割ったものと判明している。これを物理的に妥当な形（後述）に置き換えるのが最終目標。

## 2. 使用機材

| 機材 | 用途 | 分解能 |
| --- | --- | --- |
| 重量計（[YFFSFDC ポケットデジタルスケール](https://www.amazon.co.jp/dp/B07QQJ3BZG)、最大500g） | 噴射量（質量）測定。**主データとして使用** | 表示分解能 0.01 g（※製品ページに繰り返し精度・確度クラスの記載なし。<BR>安価なポケットスケールは表示桁ほどの実力精度が無いことがあるため、<BR>§4のとおり既知の小さな重りで実力精度を検証してから目標質量を決めること） |
| メスシリンダ | 噴射量（体積）の副次確認・比重算出 | 0.1 ml |
| RIGOL DHO804（4ch） | インジェクタ駆動波形の直接観測 | - |
| 電流プローブ | コイル電流波形の取得 | - |
| 燃料ポンプ＋レギュレータ | 実運用と同じ燃圧を再現 | - |

## 3. 事前準備（過去データからの教訓）

過去シート `燃料噴射時間・量` のレビューで判明した、繰り返してはいけない失敗点：

- [ ] **メスシリンダは小流量で信頼できない**。500発合計でも数ml程度になるPW（3ms以下）では実測比重が0.83〜0.88など非現実的な値になった。→ **体積は参考値、質量を主データとする**
- [ ] **短いPW側のデータが不足すると回帰が破綻する**。実測データ(0.5〜10ms)を素直に回帰すると切片が正になり、無効噴射時間が負（≈−2ms）という物理的にあり得ない結果になった。→ **0.2〜0.5ms付近を複数点、必ず追加する**
- [ ] **反復回数が水準ごとに不揃い**。0.5msはn=1で再現性を確認できなかった。→ **全水準で最低3回反復**
- [ ] **燃圧が記録されていなかった**。→ **試験条件として必ずログに残す**
- [ ] **500発の連続噴射中に燃圧が垂れていた疑いがある**（PWが大きいほど区間傾きが下がる傾向）。→ **バースト前後で燃圧を確認、可能ならバッテリー電圧も記録**

## 4. 試験条件

- 燃圧： 規定値（例: 0.35MPa ）に調整し、燃圧計で記録。  
  運転中に変動の可能性があるなら、燃圧も調整しながら測定する。
- 燃料温度：可能なら記録（粘性・比重に影響）
- 供給電圧：バッテリー電圧を記録（無効噴射時間は電圧依存性がある）
- パルス幅の水準：**0.2, 0.3, 0.4, 0.5, 1, 2, 3, 4, 6, 8, 10 ms** を目安に、実運用域（巡航4.0〜4.4ms、始動8.0ms）前後を厚めに、かつ無効噴射時間が埋もれないよう短パルス側を必ず含める
- 各水準で**最低3回**反復
- 1水準あたりのパルス数 N：重量計の表示分解能は0.01gだが、これは「読み取れる最小桁」であって「実力精度（繰り返し精度）」ではない点に注意する。
  - 事前チェック：既知の小さな重り（1〜2g程度、なければ硬貨等で代用可）を風袋込みで10回程度計量し、ばらつき（標準偏差 or 最大−最小）を確認する。
    これを実効分解能 `d_eff` とする（表示分解能どおり0.01g前後で揃うか、それとも0.02〜0.05g程度ばらつくかを見る）。
  - 目標質量 = `d_eff / 0.02`（誤差2%以下を狙う場合の最低質量）、できれば `d_eff / 0.01` 以上（誤差1%以下）を集める。
    `d_eff`が事前チェックで0.01gと確認できれば最低0.5g・目安1g、0.05g程度までばらつくなら最低2.5g・目安5gとなる。
  - 低PWほど1発あたりの噴射量が小さいため、Nを多めに設定する  
    （目安：先に500発の試し打ちをして1発あたりの概算量を出し、目標質量からNを逆算する）

## 5. 手順A（主）：直線近似法（重量計＋メスシリンダ）

ECUプログラム側の準備： `INJ_test`ブランチに `src/injector_test.cpp` として独立したインジェクタ試験プログラムを用意済み（`main.cpp`とは別ファイルで、コンパイル時にビルド環境を切り替えて使い分ける）。

- パルス幅／パルス数／間隔をSerialコマンド（`W`, `N`, `I`）で変更可能にし、再コンパイル無しで水準を切り替えられるようにしてある
- 試験中にキルスイッチが戻されたら即中断する安全策が入っている
- キルスイッチOFF＋スタータONの物理操作で噴射バーストを開始する従来の安全設計を維持している
- ビルド／書き込みはPlatformIOの環境切り替えで行う：
  - インジェクタ試験: `pio run -e uno_r4_minima_inj_test -t upload`
  - 実車ファーム（通常運転）: `pio run -e uno_r4_minima -t upload`

測定手順：

1. アキュームレータ・レギュレータで規定燃圧まで加圧
2. 風袋を引いた軽い容器をインジェクタ吐出先に固定し、重量計に乗せる
3. Serialでパルス幅・パルス数・間隔を設定
4. キルスイッチOFF＋スタータONでバースト実行
5. バースト終了直後に容器ごと質量を記録（開始前重量との差分＝噴射量[g]）
6. 同じ燃料をメスシリンダに素早く移し、体積[ml]も記録（蒸発損失を避けるため速やかに）
7. 同一パルス幅で3回以上繰り返す
8. 他のパルス幅水準へ移り、2〜7を繰り返す

### 5.1 測定記録シート

`document/injector_test_log_template.csv` に記録用フォーマットを用意している。1回のバースト噴射＝1行として、以下の列にその都度の実測値を記入する。

§10.6の追加調査項目を反映し、あらかじめ以下5フェーズ・全90行の測定順序とパラメータ（PW・N・間隔・目標燃圧/電圧）を埋め込み済み。**空欄は測定時に記入する列（測定日・測定者・燃料温度・バースト前後燃圧・開始/終了重量・噴射質量・密度・体積・備考の補足）**、**すでに値が入っている列は測定前に設定/確認する目標値**を表す。上から順に実施すればよい。

| フェーズ | 内容 | 行数 |
| --- | --- | ---: |
| P1: 基本PWスイープ(改訂N) | PW=0.2〜6ms（8組×N=550〜2600、§10.8のd_eff実測に基づき水準ごとにN改訂）を3回反復 | 27 |
| P2: バーストオフセット検証(N振り) | PW=4ms固定でN=100/300/1000/3000を3回反復（§10.6項目2、C_burst仮説の検証） | 12 |
| P3: 電圧依存性スイープ | PW=4ms固定、N=750、バッテリー電圧16.0→12.0Vを0.4V刻みで3回反復 | 33 |
| P4: 燃圧依存性スイープ | PW=4ms固定、N=750、燃圧0.25/0.30/0.35/0.40MPaを3回反復 | 12 |
| P5: 高PW再試験(保留) | PW=8,10msをN=150（暫定・要調整）で3回反復。**アキュムレータ／燃料供給系の増強後に実施** | 6 |

各列の意味：

| 列名 | 内容 |
| --- | --- |
| No | 通し番号 |
| フェーズ | 上表のP1〜P5。集計・グラフ化時のグルーピングに使う |
| 測定日 | 測定を実施した日付 |
| 測定者 | 実施者名 |
| 燃圧_MPa | §4の規定燃圧（バースト開始前の設定値） |
| 燃料温度_C | 燃料温度（可能なら記録） |
| バッテリー電圧_V | 供給電圧（無効噴射時間の電圧依存性の確認用） |
| パルス幅_ms | Serialコマンド`W`で設定した指令パルス幅 |
| パルス数_N | Serialコマンド`N`で設定したバースト内パルス数 |
| パルス間隔_ms | Serialコマンド`I`で設定したパルス間隔 |
| 反復回数 | 同一パルス幅水準内での何回目の試行か（最低3回） |
| バースト前燃圧_MPa | §3の教訓（燃圧垂れ疑い）対策。バースト開始直前の燃圧 |
| バースト後燃圧_MPa | バースト終了直後の燃圧 |
| 開始重量_g | 風袋引き後、バースト開始前の容器重量（通常0付近） |
| 終了重量_g | バースト終了直後の容器重量 |
| 噴射質量_g | 終了重量−開始重量（§7データ解析の主データ） |
| 密度_g/ml | 噴射質量 ÷ メスシリンダ体積（§7で比重換算に使用）<BR>参考：燃料仕様書によると、0.7763/15°C |
| 噴射体積_ml | メスシリンダでの副次確認値（§7では参考値扱い） |
| 備考 | 異常・外れ値の疑い等の自由記述 |
| Serialコマンド | 試験プログラムに送信するコマンド列（`W`, `N`, `I`）。<BR>パラメータを変更してデータを取る際の参照用 |

§7のデータ解析（1回あたり噴射量＝噴射質量÷N、PWに対する線形回帰など）は、この記録シートを集計して行う。

## 6. 手順B（補助・検証）：オシロスコープによる電流波形観測

- CH1：インジェクタ駆動端子（ドライバ段通過後、インジェクタ端子側）の電圧 → コマンド基準エッジ
- CH2：電流プローブでコイル電流波形を取得
- トリガ：CH1立下り、シングルショット。まず1ms/divで全体を見てから、立ち上がり直後を200〜500µs/divでズーム
- コイル電流の立ち上がり中に現れる**キンク（電機子リフトオフによる逆起電力の影響）**までの時間が開弁遅れ
- OFFエッジから電流が0になるまでの時間が閉弁遅れ
- 位置づけ：この結果はあくまで**手順Aの答え合わせ・物理現象の裏取り**。ECUコードの`calculatedINJ_time`基準で使うべき無効噴射時間の値は、手順Aの回帰から得る方が本質的に正しい（ドライバ段遅れも含めた正味の影響を捉えているため）

## 7. データ解析

1. 各パルス幅水準の質量データ（体積データではなく）を用いる
2. 「1回あたり噴射量[g] = 質量差 ÷ N」を算出
3. 全水準の (PW, 1回あたり噴射量) を線形回帰する
4. **切片の符号を必ず確認する**
   - 切片が負 → 妥当。t_dead = −切片／傾き で無効噴射時間が求まる
   - 切片が正 → 無効噴射時間モデルとして解釈不可。短パルス側のデータ不足を疑い、0.2〜0.4ms付近を追加測定してやり直す
5. R²を確認し、外れ値（燃圧変動・記録ミスの疑いがある点）がないか個別データも見る
6. 流量 Q [g/ms] を60000倍すれば g/min、比重で割れば ml/min

## 8. ECUコードへの反映

`gasml` の計算式を、物理的に素直な形に置き換える。

```cpp
// t_dead_us, flow_ml_per_us は本手順で実測した値
unsigned long duration_us = timeNow_INJ_OFF - timeNow_INJ_ON;
if (duration_us > t_dead_us) {
  gasml += flow_ml_per_us * (duration_us - t_dead_us);
}
```

同じ計算式が360CA安全リセット処理内にも重複しているため、そちらも合わせて置き換える。

## 9. 完了チェックリスト

- [ ] 燃圧・燃料温度・バッテリー電圧を記録した
- [ ] 短パルス側（0.2〜0.5ms）を複数点測定した
- [ ] 全水準で3回以上反復した
- [ ] 質量データを主指標として回帰した
- [ ] 回帰切片が負（＝物理的に妥当）であることを確認した
- [ ] オシロ波形による開弁遅れとの整合性を確認した
- [ ] 得られた t_dead・Q を `gasml` 計算式に反映し、360CAリセット側とも一致させた

## 10. 初回測定データの考察（2026-08-22実施分, `injector_test_log_20260822.csv`）

### 10.1 概要

- 燃圧0.32〜0.38MPa（規定0.35MPa近辺だが水準間でばらつきあり）、燃料温度25℃固定、バッテリー電圧16.2〜16.3V、PW=0.2〜10msの11水準×3回、**N=300固定**で実施（行1〜33）。
- PW=8, 10msは備考欄に「ダイヤフラムポンプ（2cc）容積オーバー」と記録されており、実施者自身が燃料供給不足（§3で指摘した燃圧垂れと同種の問題）を検知済み。**以降の解析ではこの2水準を除外**する。
- PW=4ms固定でのバッテリー電圧スイープ（16.2V→12.0Vを0.4V刻み、各3回、行34〜66）が計画済みだが、本稿執筆時点では未計測（空欄）。

### 10.2 水準別サマリ

| PW[ms] | N | 平均噴射量[g] | 1発あたり[mg] | 判定 |
| ---: | ---: | ---: | ---: | --- |
| 0.2 | 300 | 0.290 | 0.967 | 分解能に対し噴射量が小さくデータが荒い |
| 0.3 | 300 | 0.337 | 1.122 | 同上 |
| 0.4 | 300 | 0.407 | 1.356 | 同上 |
| 0.5 | 300 | 0.427 | 1.422 | 同上 |
| 1.0 | 300 | 0.530 | 1.767 | 直線性良好域 |
| 2.0 | 300 | 0.697 | 2.322 | 直線性良好域 |
| 3.0 | 300 | 0.853 | 2.844 | 直線性良好域 |
| 4.0 | 300 | 1.027 | 3.422 | 直線性良好域 |
| 6.0 | 300 | 1.357 | 4.522 | 直線性良好域（区間傾きがわずかに低下し始める） |
| 8.0 | 300 | 1.593 | 5.311 | **除外**（燃料供給不足） |
| 10.0 | 300 | 1.790 | 5.967 | **除外**（燃料供給不足） |

区間傾き（隣接水準間の Δ噴射量/ΔPW）を見ると、1〜6ms間は 0.157〜0.173 g/ms/300発で安定しているのに対し、6→8ms間は0.118、8→10ms間は0.098まで低下しており、備考の「容積オーバー」の記載と定量的にも整合する。

### 10.3 回帰結果

PW=0.2〜6msの27点（8,10msを除く全水準）で単回帰すると：

```txt
噴射量[g/300発] = 0.1767 × PW[ms] + 0.3165　　R² = 0.988
```

**切片が正**であり、t_dead = −0.3165 / 0.1767 ≈ **−1.8 ms** と物理的にあり得ない値になった。§3で報告済みの過去の失敗（切片正・t_dead≈−2ms）とほぼ同じ症状であり、今回0.2〜0.5msを追加しても解消していない。

### 10.4 原因の切り分け

- 逆にノイズの大きい0.2〜0.5msを外し、直線性の良い**PW=1〜6msのみ（15点）**で再回帰すると R²=0.992 と非常に良い当てはまりになる一方、t_dead ≈ **−2.2 ms** とむしろ悪化する。  
  → 単に短パルス側のデータが荒いだけでは説明できない。
- 使用した重量計（[YFFSFDC ポケットデジタルスケール](https://www.amazon.co.jp/dp/B07QQJ3BZG)）の表示分解能は実際には**0.01g**（§2参照。当初この考察で前提にしていた0.1gは誤り）。  
  この分解能を基準にすると、PW=1〜6msの各水準（噴射量1.03〜1.36g、N=300）は§4改訂後の目安（最低0.5g・目安1g以上）を十分満たしており、**Nはこの範囲では大きな問題ではなかった**可能性が高い。  
  不足が疑わしいのはPW=0.2〜0.5ms（噴射量0.29〜0.43g）のみ。
- PW=1〜6msの分解能に対して十分な質量が確保されていたにもかかわらずR²=0.992・切片正という結果が出ていることは、**単純な量子化誤差では説明しにくい**ことを意味する。  
  むしろ「**バーストごとに一定量のオフセットが乗っている**」（噴射量 = N×Q×(PW−t_dead) + C_burst、C_burstはバースト開始時のインジェクタ挙動やアキュムレータ圧の過渡応答による余剰噴射などPWによらない固定量）という仮説、あるいは重量計の実力精度が表示分解能（0.01g）より悪い（§4改訂で追記した「実効分解能 d_eff」の検証が未実施）という仮説の方が有力になった。

### 10.5 導出できたパラメータ（暫定・要検証）

- **流量 Q ≈ 33〜35 g/min ≈ 44〜47 ml/min**（密度0.754g/ml換算、燃圧0.33〜0.38MPa条件下、PW=0.2〜6msでの回帰による）。直線性は良好（R²=0.99前後）なため、Qの推定値としては信頼できる。
- **無効噴射時間 t_dead：有効な値は得られていない**（回帰切片が正のため）。§9のチェックリスト「回帰切片が負であることを確認」を満たしておらず、このまま`gasml`計算式（§8）には反映できない。

### 10.6 追加で調査すべき項目

1. ~~重量計の実効分解能 `d_eff` を検証する~~：解決済み（§10.8）。1円硬貨10回計量の結果、`d_eff ≈ 0.02〜0.03g`（表示分解能0.01gよりはやや悪いが良好）。これを踏まえてNを再設計する：PW=0.2〜0.5msはN≈1800〜2600、PW=1〜2msはN≈1100〜1400、PW=3〜4msはN≈750〜900、PW=6msはN≈550程度に引き上げる（詳細は§10.8の表を参照）。
2. **バーストごとの固定オフセットの有無を検証する**：同一PW（例: 4ms）でNだけを100/300/1000/3000のように変えて噴射量をプロットし、N→0への外挿がゼロを通るか確認する。ゼロを通らない場合、10.4の「C_burst」仮説が支持される。10.4の分析より、こちらが現時点で最も有力な仮説であり優先度を上げて検証する。
3. **開始重量・終了重量、バースト前後燃圧を毎回記録する**：現状すべて空欄で噴射量の計算値のみが残っており、生データの検証や燃圧垂れの定量評価ができない。`injector_test_log_template.csv`の該当列を今後は必ず埋める。
4. **PW=8, 10msの再試験（燃料供給系の見直し後）**：ダイヤフラムポンプ／アキュムレータの容量不足が疑われるため、より大容量のアキュムレータに変更するか、高PW域だけNを絞って再測定する。
5. **バッテリー電圧依存性の測定を完遂する**（行34〜66に計画済み）：PW=4ms固定で16.2V→12.0Vをスイープする計画は§4の要求（無効噴射時間の電圧依存性）に合致しており妥当。実施時は項目1（N増量）・項目3（開始/終了重量・燃圧の記録）も適用すること。
6. **燃圧依存性の測定**：現状すべて0.32〜0.38MPaに集中しており、Q・t_deadの√ΔP依存性は未検証。レギュレータで燃圧を複数水準（例: 0.25/0.30/0.35/0.40MPa）に振った測定を追加すると、実運用時の燃圧変動に対する頑健性を確認できる。
7. **密度の測定方法を記録する**：噴射体積列は「噴射質量 ÷ 密度(0.754固定)」で計算されており、独立測定ではない。§3の教訓（小流量でのメスシリンダ不信頼）を踏まえると妥当な処置だが、密度0.754の測定方法（温度・測定器）を記録し、燃料仕様書値（0.7763@15℃）との差が温度換算で妥当か確認する。
8. **手順Bとの突き合わせ**：オシロによる開弁遅れ計測（§6）をPW=4ms付近で一度実施し、t_dead再測定値と桁が合うか照合する。
9. ~~重量計の分解能表記の見直し~~：解決済み。製品ページ（[YFFSFDC ポケットデジタルスケール](https://www.amazon.co.jp/dp/B07QQJ3BZG)）で表示分解能0.01gを確認し、§2・§4に反映した。ただし製品ページに確度・繰り返し精度の記載はなく、実力精度は未検証（→項目1）。

### 10.7 まとめ

現時点のデータは**Qの推定（約44〜47 ml/min、R²≈0.99の良好な直線性）には十分使えるが、t_dead（無効噴射時間）は依然として物理的に妥当な値が得られていない**。§9のチェックリストにある「回帰切片が負であることを確認」を満たしていないため、`gasml`計算式（§8）への反映はまだ行わないこと。重量計の実際の分解能（0.01g）を踏まえると、PW=1〜6msでは単純なN不足では説明しづらく、10.6項目2の「バーストごとの固定オフセット」の方が有力な仮説である。10.6の項目、特に1（重量計の実力精度検証）と2（バーストオフセットの切り分け）を優先して再測定することを推奨する。

### 10.8 重量計 実効分解能の検証結果（1円硬貨10回計量）

§4の事前チェックを実施した結果：

```txt
0.98, 1.00, 0.98, 1.00, 0.99, 1.00, 1.00, 0.99, 0.98, 1.01 [g]（公称1.000gの1円硬貨、n=10）
```

- 平均 0.993g、標準偏差 ≈0.011g、レンジ(最大−最小) 0.03g（0.98〜1.01g）
- **繰り返し精度は表示分解能どおりほぼ0.01g級で良好**（安価なスケールとしては上出来）。ただし1回の読み取り単体では±0.01〜0.03g程度ばらつき得るため、実効分解能は `d_eff ≈ 0.02〜0.03g`（レンジを目安）として扱うのが安全側。
- 平均が公称値より約0.007g（0.7%）低い、**わずかな系統的なマイナスオフセット**が見られる。ただしこの種の一律オフセットは回帰の傾き・切片に比例的にかかるだけなので、t_dead=−切片/傾きという**比の値には影響しない**（10.4の切片正問題の説明にはならない）。気になる場合は今後、実測値を×(1.000/0.993)≈1.00705倍する補正、または計量前に既知の重りでスパン点を確認する運用を追加するとよい。

`d_eff ≈ 0.02〜0.03g` を§4の式に当てはめると、最低質量 ≈1.0〜1.5g、目安質量 ≈2.0〜3.0g となる。10.2の実測値（PW=6msでも約1.36g）と比較すると、**現行N=300ではPW=6ms以下のほぼ全水準で目安質量に届いていない**。10.6項目1のN引き上げは、以下を目安に設定する。

| PW[ms] | 最低ライン相当のN(≈1.25g) | 目安相当のN(≈2.5g) |
| ---: | ---: | ---: |
| 0.2〜0.5 | 900〜1300 | 1800〜2600 |
| 1〜2 | 550〜700 | 1100〜1400 |
| 3〜4 | 400〜450 | 750〜900 |
| 6 | 300弱 | 約550 |
